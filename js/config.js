// Own copy, not shared with check-user-'s js/config.js — this repo has no
// file that imports from, or is imported by, check-user-. Fill in the SAME
// Supabase project api-layer-unified uses.

window.PLATFORM_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
  API_BASE: 'http://localhost:4000',
};
